<% provide(:title, 'Contacts') %>
<h1>My Contacts</h1>

<%= link_to "Add New Contact", new_contact_path, class: "btn btn-large btn-default pull-right" %>

<%= alphabetical_paginate @alphaParams %>

<div id="pagination_table">
  <ul class="list-unstyled">
    <%= render @contacts %>
  </ul>
</div>

<!-- <table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Email</th>
      <th>Phone</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.name %></td>
        <td><%= contact.email %></td>
        <td><%= contact.phone %></td>
        <td><%= contact.user.id %></td>
        <td><%= link_to 'Show', contact %></td>
        <td><%= link_to 'Edit', edit_contact_path(contact) %></td>
        <td><%= link_to 'Destroy', contact, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Contact', new_contact_path %> -->
